# 开发过程记录

## 一、项目概述

英语跟读评分 Demo，核心流程：用户输入英文句子 → 程序朗读 → 用户跟读录音 → 语音识别 → 与原句比对评分。纯前端单页应用，零后端、零依赖、零费用。

- **仓库**：https://github.com/Huber08/english-speaking-demo
- **在线访问**：https://huber08.github.io/english-speaking-demo/
- **技术栈**：HTML / CSS / 原生 JavaScript + 浏览器 Web Speech API

---

## 二、实际耗时

| 阶段 | 耗时 |
|---|---|
| 方案选型与技术调研 | 约 0.5 小时 |
| 核心流程编码（TTS / 录音 / 识别 / 评分） | 约 2 小时 |
| UI 与交互优化（音量条、逐词高亮、逐词对比） | 约 1 小时 |
| 问题排查与修复（录音无响应、音色选择、移动端适配） | 约 2 小时 |
| 部署与文档整理 | 约 0.5 小时 |
| **合计** | **约 6 小时** |

---

## 三、遇到的主要难点与解决思路

### 难点 1：语音识别在国内网络环境下的可用性

**问题**：浏览器原生 `webkitSpeechRecognition` 在 Chrome 中走 Google 语音服务，国内网络无法访问，导致识别无响应。

**尝试过的思路**：
1. 接入百度 / 讯飞 / 阿里云的语音识别 API —— 但都需要注册账号、申请密钥且有免费额度限制，不符合"免费、跑通"的要求。
2. 使用 Vosk / whisper.cpp 做本地离线识别 —— 体积大、集成复杂，不适合单文件 Demo。

**最终方案**：使用浏览器原生 `webkitSpeechRecognition`，但在页面明确提示用户使用 **Microsoft Edge**（Edge 底层走微软语音服务，国内直连可用）。在识别失败的 `onerror` 回调中，针对 `network` 错误给出"改用 Edge"的引导。

---

### 难点 2：点击录音按钮"没反应"

**问题**：用户反馈点麦克风没有任何反应。

**根因分析**：
1. 用户在 IDE 的内置预览窗口（Electron 环境）中打开，该环境不支持麦克风，也不弹权限询问；
2. 原代码在 `getUserMedia` 异步返回前没有任何界面反馈，导致用户以为按钮坏了；
3. 错误提示只是一行小字，不醒目。

**修复方案**：
1. 页面加载时做环境自检，检测到内置预览窗口 / 非安全上下文 / 无录音能力时，顶部黄条明确提示并提供"复制地址"按钮；
2. 点击麦克风后立即给可见反馈（按钮变橙色 + "正在请求麦克风权限"状态文案），避免"无响应空窗期"；
3. 麦克风错误分类展示：权限被拒 🔒、设备未找到 🎧、被占用 🎙，均有醒目红底提示；
4. 超过 4 秒未授权，提示用户去地址栏锁形图标处放行。

---

### 难点 3：美式 / 英式发音切换无效、只有女声

**问题**：用户在手机 Edge 上访问，切换口音和性别都没效果，只有一个女声。

**根因**：移动端（特别是 iOS，所有浏览器强制 WebKit 内核）的 SpeechSynthesis 语音库极度受限，通常只有 1 个英文语音，且 `voiceschanged` 事件经常不触发。

**修复方案**：
1. 新增"设备语音"下拉，设备有多个英文语音时可手动选择；
2. 在控件下方显示当前实际生效的音色名和可用语音数量；
3. 只有 1 个英文语音时，禁用性别下拉并明确提示"设备未安装对应语音"；
4. 增加延时轮询兜底（200ms / 600ms / 1500ms / 3000ms），解决部分环境 `voiceschanged` 不触发的问题。

---

### 难点 4：评分算法的设计

**问题**：如何衡量"跟读得好不好"。

**尝试过的思路**：
1. 音素级发音评分 —— 需要强制对齐 + 音素级声学模型，通常要调付费 API，放弃。
2. 字符级编辑距离 —— 对单词拼写错误敏感，不适合衡量"读对几个词"。

**最终方案**：词级 Levenshtein 编辑距离对齐。
- 归一化（小写、去标点）后按词切分；
- 动态规划计算最小编辑路径，回溯得到逐词操作；
- 四类错误：读对（equal）、读错（sub）、漏读（del）、多读（ins）；
- 得分 = 读对词数 / 原句词数 × 100，同时给出 WER 词错率。

---

## 四、关键判断与假设

| 判断 / 假设 | 依据 |
|---|---|
| 优先用 Edge 浏览器 | Chrome 的识别服务在国内不可用，Edge 走微软服务可直连 |
| 用 HTTPS / localhost 部署 | 浏览器仅在安全上下文下开放麦克风权限 |
| 评分基于识别文本而非音频 | 免费方案下无法做音素级发音评估，退而求其次衡量"内容读得对不对、全不全" |
| 语音选择用启发式性别判断 | Web Speech API 不直接返回 voice 的性别属性，只能根据 voice.name 关键词推断 |
| 单文件部署 | 降低使用门槛，面试官打开链接即可体验 |

---

## 五、已知局限

1. **评分不是专业发音分**：基于识别文本与原句的词级对齐，衡量"读得对不对、全不全"，不评估音素准确度、流利度、语调。
2. **同义/缩写差异判错**：如 `can't` vs `cannot`、同义词替换会被判为不一致。
3. **识别准确率受环境影响**：麦克风质量、环境噪音会影响识别结果，进而影响评分。
4. **移动端语音受限**：iOS 设备英文语音通常只有 1 个，无法选择口音 / 性别。
5. **实时识别依赖网络**：Edge 的识别服务需要联网，离线不可用。

---

## 六、技术方案总结

```
输入句子 ──SpeechSynthesis──→ 示范朗读（逐词高亮）
                                    │
                                    ▼ 用户跟读
麦克风 ──MediaRecorder──→ 录音文件（音量条 / 回放）
   │
   └──webkitSpeechRecognition──→ 识别文本
                                    │
                                    ▼
                    词级 Levenshtein 对齐 ──→ 得分 + WER + 逐词对比
```

所有重活（语音合成、语音识别）交给浏览器原生 API，自己只负责异步 API 的状态编排和评分算法。
