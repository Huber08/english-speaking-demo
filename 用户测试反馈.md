# 用户测试反馈

## 测试概述

邀请了 2 位用户试用本 Demo，记录其使用过程、遇到的问题与评价。

| 测试者 | 背景 | 使用环境 |
|---|---|---|
| 用户 A | 大学生，英语水平中等 | Windows 11 + Microsoft Edge，本地 `localhost` |
| 用户 B | 在职程序员，英语水平良好 | Windows 10 + Microsoft Edge，Vercel 部署的 HTTPS 地址 |

---

## 用户 A 测试记录

### 使用过程

1. 打开页面，看到三步卡片界面，直接点击示例句子填入文本框。
2. 点击"朗读句子"，听到女声朗读，句子区域逐词高亮。
3. 点击麦克风，浏览器弹出权限询问，点击"允许"。
4. 跟读句子，音量条有跳动，实时识别区显示转写文字。
5. 再次点击麦克风停止，自动出分，展示逐词对比。

### 遇到的问题

1. **录音按钮第一次点击时没反应**：用户在 IDE 内置预览窗口打开，页面顶部没有提示。→ 已修复：增加环境自检黄条。
2. **不知道要再点一次麦克风停止**：以为录音会自动结束，等了几秒没反应。→ 已优化：状态文案明确提示"跟读完后再次点击麦克风停止"。
3. **得分比预期低**：读对了但识别把 "seashells" 识别成 "sea shells"，被判为读错。→ 这是识别准确率问题，已在结果区说明"基于识别文本，不等同于专业发音分"。

### 评价

> "界面很清爽，操作流程直观，逐词对比能清楚看到哪里读错了。音量条和实时字幕让我知道录音在正常工作，很有安全感。"

### 改进建议

- 录音时增加倒计时或自动停止（如 10 秒无声音自动结束）；
- 得分区域可以增加一个"重新朗读"按钮，不用滚动回顶部。

---

## 用户 B 测试记录

### 使用过程

1. 直接访问 Vercel 部署的 HTTPS 链接，页面加载正常。
2. 输入自定义句子，选择英式发音 + 女声，点击朗读。
3. 跟读并录音，出分后查看逐词对比。
4. 切换美式 / 英式、女声 / 男声，对比发音差异。
5. 尝试录音回放功能。

### 遇到的问题

1. **手机 Edge 上切换男女声没效果**：只有一个女声。→ 已修复：显示设备可用语音数量，单语音时禁用选择并提示。
2. **英式发音听起来和美式一样**：系统未安装英式语音，自动回退到美式。→ 已修复：在音色提示中明确告知"系统未安装英式语音，已自动回退"。

### 评价

> "作为一个 Demo，核心流程跑得很顺。评分逻辑透明，能看到每个词的对错，比单纯给一个分数有说服力。技术选型很聪明，全用浏览器原生 API，零成本。"

### 改进建议

- 增加历史成绩记录，能看到进步曲线；
- 支持选择不同难度的句子库，不用手动输入；
- 录音回放可以变速播放，方便对比自己和示范的差异。

---

## 改进建议汇总

| 优先级 | 建议 | 说明 |
|---|---|---|
| 高 | 自动停止录音 | 静音检测或倒计时，避免用户忘记停止 |
| 中 | 历史成绩记录 | localStorage 存储，展示进步曲线 |
| 中 | 句子库 | 内置不同难度的句子，一键选择 |
| 低 | 录音回放变速 | 0.5x / 1.0x / 1.5x 对比 |
| 低 | 发音专项反馈 | 针对读错的词给出发音建议 |

---

## 结论

两位用户均能在**无指导的情况下独立完成核心流程**，说明交互设计基本达标。主要问题集中在**环境适配**（内置预览、移动端语音受限）和**评分认知**（用户期望专业发音分），已通过环境自检、明确提示和结果说明进行改善。
